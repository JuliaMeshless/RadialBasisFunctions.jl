import{_ as o,c as t,o as a,aA as n}from"./chunks/framework.CLrzXryn.js";const h=JSON.parse('{"title":"Convergence &amp; Parameter Selection","description":"","frontmatter":{},"headers":[],"relativePath":"guides/convergence/index.md","filePath":"guides/convergence/index.md","lastUpdated":null}'),r={name:"guides/convergence/index.md"};function i(s,e,c,d,l,p){return a(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="Convergence-and-Parameter-Selection" tabindex="-1">Convergence &amp; Parameter Selection <a class="header-anchor" href="#Convergence-and-Parameter-Selection" aria-label="Permalink to &quot;Convergence &amp;amp; Parameter Selection {#Convergence-and-Parameter-Selection}&quot;">​</a></h1><p>This guide documents how interpolation and differential-operator accuracy depend on the point cloud, basis function, polynomial augmentation degree, and stencil size — with convergence and work-precision plots generated from offline sweeps across every basis (PHS1, PHS3, PHS5, PHS7, IMQ, Gaussian) and polynomial degree combination.</p><p>Use it as a reference when picking parameters for your own problems.</p><h2 id="Pages" tabindex="-1">Pages <a class="header-anchor" href="#Pages" aria-label="Permalink to &quot;Pages {#Pages}&quot;">​</a></h2><ul><li><p><strong><a href="./scalar-operators">Scalar Operators</a></strong> — convergence for <code>Interpolator</code>, <code>laplacian</code>, <code>gradient</code>, <code>partial</code>, and mixed partials.</p></li><li><p><strong><a href="./vector-operators">Vector Operators</a></strong> — convergence for <code>hessian</code>, <code>jacobian</code>, <code>divergence</code>, <code>curl</code>.</p></li><li><p><strong><a href="./refinement-sweeps">Refinement Sweeps</a></strong> — polynomial degree (p-refinement), stencil size (k-refinement), and shape parameter (ε-refinement) behavior across all basis functions.</p></li><li><p><strong><a href="./work-precision">Work-Precision</a></strong> — the computational cost of every (basis, polynomial degree) combination: build time, apply time, and memory vs accuracy.</p></li><li><p><strong><a href="./3d">3D Extensions</a></strong> — convergence plots for 3D problems.</p></li></ul><h2 id="Notation-and-definitions" tabindex="-1">Notation &amp; definitions <a class="header-anchor" href="#Notation-and-definitions" aria-label="Permalink to &quot;Notation &amp;amp; definitions {#Notation-and-definitions}&quot;">​</a></h2><ul><li><p><strong>h-refinement</strong>: refine the point cloud (increase <code>N</code>). In 2D, typical spacing <code>h ∼ 1/√N</code>. Primary convergence test.</p></li><li><p><strong>p-refinement</strong>: vary <code>poly_deg</code> at fixed <code>N</code>.</p></li><li><p><strong>k-refinement</strong>: vary stencil size at fixed <code>N</code> and <code>poly_deg</code>.</p></li><li><p><strong>ε-refinement</strong>: vary the shape parameter of <code>IMQ</code> or <code>Gaussian</code> at fixed everything else.</p></li><li><p><strong>NRMSE</strong>: normalized RMSE, <code>√(Σ(computed − exact)² / Σ exact²)</code>. Dimensionless; comparable across problems.</p></li></ul><h2 id="Expected-convergence-rates" tabindex="-1">Expected convergence rates <a class="header-anchor" href="#Expected-convergence-rates" aria-label="Permalink to &quot;Expected convergence rates {#Expected-convergence-rates}&quot;">​</a></h2><p>With polynomial augmentation of degree <code>m</code> and a sufficiently smooth target, local-stencil RBF-FD approximations converge at:</p><ul><li><p><strong>Interpolation</strong>: <code>O(h^{m+1})</code></p></li><li><p><strong>First derivatives</strong>: <code>O(h^m)</code></p></li><li><p><strong>Second derivatives</strong>: <code>O(h^{m-1})</code></p></li></ul><p>For PHS, the standard rule is <code>poly_deg = ⌈n/2⌉</code> for PHS(n) as the minimum that achieves the expected rate: PHS1 → p=1, PHS3 → p=2, PHS5 → p=3, PHS7 → p=4. Higher <code>poly_deg</code> adds more monomials and can further boost the observed rate up to a point of diminishing returns.</p><p>For IMQ and Gaussian, the shape parameter <code>ε</code> controls the basis width; smaller <code>ε</code> is flatter (more accurate in principle) but conditioning degrades — see the <a href="/RadialBasisFunctions.jl/previews/PR125/guides/convergence/refinement-sweeps#shape-parameter-epsilon-refinement">ε-refinement section</a>.</p><h2 id="How-to-read-the-plots" tabindex="-1">How to read the plots <a class="header-anchor" href="#How-to-read-the-plots" aria-label="Permalink to &quot;How to read the plots {#How-to-read-the-plots}&quot;">​</a></h2><p>All convergence plots are log-log NRMSE vs <code>N</code> (number of points). Dashed gray lines show reference slopes <code>O(h^p)</code> for visual comparison. Each colored curve is one (basis, polynomial degree) configuration.</p><p>Work-precision plots show NRMSE vs wall-clock time — lower-left is better. A point cloud that&#39;s &quot;cheaper per error&quot; is closer to the bottom-left corner.</p><div class="tip custom-block"><p class="custom-block-title">Unusable combinations are omitted</p><p>Some <code>(basis, poly_deg)</code> pairs do not converge for certain operators — most notably PHS1 with any second-derivative operator, and PHS3/p=2 with mixed partials or the full Hessian. These combinations are <strong>omitted from plots</strong> rather than shown as flat lines, with a warning admonition at the top of each affected operator section listing exactly what&#39;s excluded and why.</p></div><h2 id="Methodology" tabindex="-1">Methodology <a class="header-anchor" href="#Methodology" aria-label="Permalink to &quot;Methodology {#Methodology}&quot;">​</a></h2><ul><li><p><strong>Test function for interpolation</strong>: Franke (1979), a standard test with multiple length scales.</p></li><li><p><strong>Test function for differential operators</strong>: <code>g(x) = 1 + sin(4x₁) + cos(3x₁) + sin(2x₂)</code> with analytic derivatives.</p></li><li><p><strong>Point cloud</strong>: jittered grid on <code>[0,1]²</code>, seed=42 for reproducibility.</p></li><li><p><strong>Sweep</strong>: <code>n_side ∈ {10, 15, 20, 30, 45, 70}</code>, giving <code>N ∈ {100, 225, 400, 900, 2025, 4900}</code>.</p></li><li><p><strong>Error metric</strong>: NRMSE on the training points (collocation residual) for differential operators; NRMSE on 500 held-out points for interpolation.</p></li><li><p><strong>Regeneration</strong>: all plots come from committed CSVs under <code>docs/src/assets/convergence/data/</code>. To refresh, run <code>julia --project=docs docs/src/assets/convergence/generate_data.jl</code> followed by <code>generate_plots.jl</code>.</p></li></ul>',18)])])}const m=o(r,[["render",i]]);export{h as __pageData,m as default};
