import{_ as a,c as t,o,aA as i}from"./chunks/framework.CATvnsXa.js";const r="/RadialBasisFunctions.jl/previews/PR137/assets/interpolation_imq_gaussian_eps_compare.Cr3D0KI-.png",s="/RadialBasisFunctions.jl/previews/PR137/assets/laplacian_imq_gaussian_eps_compare.DZFp-0ev.png",n="/RadialBasisFunctions.jl/previews/PR137/assets/mixed_partial_imq_gaussian_eps_compare.sXBDvkNF.png",f=JSON.parse('{"title":"Shape-Parameter Bases (IMQ, Gaussian)","description":"","frontmatter":{},"headers":[],"relativePath":"guides/convergence/shape-parameter-bases.md","filePath":"guides/convergence/shape-parameter-bases.md","lastUpdated":null}'),c={name:"guides/convergence/shape-parameter-bases.md"};function d(l,e,p,h,g,m){return o(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="Shape-Parameter-Bases-IMQ,-Gaussian" tabindex="-1">Shape-Parameter Bases (IMQ, Gaussian) <a class="header-anchor" href="#Shape-Parameter-Bases-IMQ,-Gaussian" aria-label="Permalink to &quot;Shape-Parameter Bases (IMQ, Gaussian) {#Shape-Parameter-Bases-IMQ,-Gaussian}&quot;">​</a></h1><p>Polyharmonic splines have no shape parameter and converge cleanly under h-refinement. The shape-parameter bases — <code>IMQ</code> and <code>Gaussian</code> — do not: their h-refinement behavior is dominated by the interaction between ε and the stencil spacing, and fixing ε across a refinement sweep causes observable error growth at large <code>N</code>. This page explains why and shows what cleans it up.</p><p>All <code>IMQ</code> / <code>Gaussian</code> h-refinement plots have been moved here so the operator pages can stay focused on rate / poly_deg comparisons.</p><h2 id="The-RBF-uncertainty-principle" tabindex="-1">The RBF uncertainty principle <a class="header-anchor" href="#The-RBF-uncertainty-principle" aria-label="Permalink to &quot;The RBF uncertainty principle {#The-RBF-uncertainty-principle}&quot;">​</a></h2><p>For a stencil of physical radius <code>h</code>, the basis <code>ϕ(r) = exp(-(εr)²)</code> (Gaussian) or <code>ϕ(r) = 1/√(1+(εr)²)</code> (IMQ) is characterized entirely by the non-dimensional product <code>ε·h</code>. Two limits:</p><ul><li><p><code>ε·h → ∞</code> (peaked basis, large ε relative to spacing): the RBF decays to zero between neighbors. The local system approaches a diagonal interpolant with poor approximation power.</p></li><li><p><code>ε·h → 0</code> (flat basis, small ε relative to spacing): every RBF evaluates to nearly the same value across the stencil. The RBF columns of the collocation matrix lose linear independence, the system is ill-conditioned, and the solved stencil weights grow large with alternating signs. Multiplying those weights through the evaluation amplifies floating-point roundoff error.</p></li></ul><p>Truncation error (analytic approximation error) shrinks as <code>ε·h → 0</code>, but rounding error grows. The two cross over at a problem-specific sweet spot — the <strong>trade-off principle</strong> of Schaback [1], revisited by Driscoll &amp; Fornberg [2] and Fornberg &amp; Flyer [3]. An ε-refinement sweep at fixed <code>N</code> shows this directly as a U-shape in NRMSE vs ε (see <code>data/eps_refinement.csv</code>).</p><p>Now consider h-refinement with ε held constant. Stencil size <code>k</code> is fixed per configuration, so as <code>N</code> grows the physical stencil radius shrinks as <code>h ~ 1/√N</code>. The product <code>ε·h</code> collapses toward zero, driving the system into the ill-conditioned regime. Error first decreases (truncation dominates), then bottoms out and rises (rounding dominates). This is the &quot;fixed ε&quot; story below.</p><p>PHS is immune: it has no shape parameter and is scale-invariant when paired with polynomial augmentation, so stencil shrinkage doesn&#39;t change conditioning.</p><h2 id="Fixed-ε-1-vs-scaled-ε-c/h" tabindex="-1">Fixed ε = 1 vs scaled ε = c/h <a class="header-anchor" href="#Fixed-ε-1-vs-scaled-ε-c/h" aria-label="Permalink to &quot;Fixed ε = 1 vs scaled ε = c/h {#Fixed-ε-1-vs-scaled-ε-c/h}&quot;">​</a></h2><p>The fix is to scale ε so that <code>ε·h</code> stays near the sweet spot as <code>N</code> grows. Stationary scaling <code>ε = c/h</code> keeps the non-dimensional basis shape constant across the sweep. The constants <code>c</code> below are calibrated from the fixed-<code>N</code> ε-refinement minima in <code>eps_refinement.csv</code> at <code>N=900</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Operator</th><th style="text-align:right;"><code>c</code></th><th style="text-align:right;">Rationale</th></tr></thead><tbody><tr><td style="text-align:right;">Interpolation</td><td style="text-align:right;">0.15</td><td style="text-align:right;">ε_opt ≈ 3–6 at <code>h = 1/30</code> → <code>ε·h ≈ 0.10–0.21</code></td></tr><tr><td style="text-align:right;">Laplacian</td><td style="text-align:right;">0.03</td><td style="text-align:right;">ε_opt ≈ 0.5–1.0 → <code>ε·h ≈ 0.017–0.033</code></td></tr><tr><td style="text-align:right;">Mixed partial</td><td style="text-align:right;">0.03</td><td style="text-align:right;">Same order (2nd derivative), same scaling</td></tr></tbody></table><p>Each plot below shows the same operator twice:</p><ul><li><p><strong>Left:</strong> <code>IMQ</code> and <code>Gaussian</code> with <code>ε = 1</code> — the standard curves from the <code>h_refinement.csv</code> sweep, reproduced here.</p></li><li><p><strong>Right:</strong> <code>IMQ</code> and <code>Gaussian</code> with <code>ε = c/h</code> — a new sweep (<code>hseps</code> target in <code>generate_data.jl</code>) that holds <code>ε·h</code> constant.</p></li></ul><p>A <code>PHS5/p=3</code> curve from the matched-degree sweep appears on both panels as a non-shape-parameter reference. The y-axis is shared across panels.</p><h3 id="Interpolation" tabindex="-1">Interpolation <a class="header-anchor" href="#Interpolation" aria-label="Permalink to &quot;Interpolation {#Interpolation}&quot;">​</a></h3><p><img src="'+r+'" alt=""></p><p>The fixed-ε panel shows non-monotone behavior and poly_deg-dependent stagnation. Scaled ε recovers clean convergence; at large <code>N</code> the shape-parameter bases beat the PHS5/p=3 reference by one to two orders of magnitude because a well- tuned Gaussian or IMQ approaches spectral accuracy on smooth targets.</p><h3 id="Laplacian" tabindex="-1">Laplacian <a class="header-anchor" href="#Laplacian" aria-label="Permalink to &quot;Laplacian {#Laplacian}&quot;">​</a></h3><p><img src="'+s+'" alt=""></p><p>At <code>ε = 1</code> the <code>p=2</code> curves flatten or turn up past <code>N ≈ 2000</code>; scaled ε restores steady <code>~O(h²)–O(h³)</code> convergence, with <code>p=3</code> matching or beating PHS5/p=3.</p><h3 id="Mixed-partial" tabindex="-1">Mixed partial <a class="header-anchor" href="#Mixed-partial" aria-label="Permalink to &quot;Mixed partial {#Mixed-partial}&quot;">​</a></h3><p><img src="'+n+'" alt=""></p><p>Only <code>p=3</code> is shown — <code>p=0</code> and <code>p=2</code> don&#39;t converge for mixed partials with shape-parameter bases regardless of ε (a polynomial-augmentation issue, not a conditioning one — see the <a href="/RadialBasisFunctions.jl/previews/PR137/guides/convergence/scalar-operators#mixed-partial-xxj">mixed partial section</a>). With <code>p=3</code>, fixed ε shows the classic turnaround around <code>N ≈ 2000</code>; scaled ε converges cleanly.</p><h2 id="Practical-guidance" tabindex="-1">Practical guidance <a class="header-anchor" href="#Practical-guidance" aria-label="Permalink to &quot;Practical guidance {#Practical-guidance}&quot;">​</a></h2><ul><li><p><strong>PHS is the default.</strong> No parameter tuning, no conditioning cliff. Use <code>PHS(3; poly_deg=2)</code> or <code>PHS(5; poly_deg=3)</code> unless a specific reason motivates shape-parameter bases.</p></li><li><p><strong>If you use <code>IMQ</code> or <code>Gaussian</code>:</strong> scale ε with the local stencil spacing. For a quasi-uniform point cloud, <code>ε ≈ c / h</code> with <code>c</code> calibrated once via an ε-refinement sweep at representative <code>N</code> is straightforward. Do not carry a single ε across problems of different resolution without retuning.</p></li><li><p><strong>Stable evaluation.</strong> Algorithms like RBF-QR [4] and Contour–Padé [5] compute stable weights across the full ε range without the uncertainty principle, but are not currently implemented in this package.</p></li></ul><h2 id="References" tabindex="-1">References <a class="header-anchor" href="#References" aria-label="Permalink to &quot;References {#References}&quot;">​</a></h2><ol><li><p><strong>Schaback, R.</strong> (1995). <em>Error estimates and condition numbers for radial basis function interpolation.</em> Advances in Computational Mathematics, <strong>3</strong>, 251–264. DOI: <a href="https://doi.org/10.1007/BF02432002" target="_blank" rel="noreferrer">10.1007/BF02432002</a>. <em>Original formulation of the trade-off / uncertainty principle.</em></p></li><li><p><strong>Driscoll, T. A. and Fornberg, B.</strong> (2002). <em>Interpolation in the limit of increasingly flat radial basis functions.</em> Computers &amp; Mathematics with Applications, <strong>43</strong>(3–5), 413–422. DOI: <a href="https://doi.org/10.1016/S0898-1221(01)00295-4" target="_blank" rel="noreferrer">10.1016/S0898-1221(01)00295-4</a>.</p></li><li><p><strong>Fornberg, B. and Flyer, N.</strong> (2015). <em>A Primer on Radial Basis Functions with Applications to the Geosciences.</em> CBMS-NSF Regional Conference Series in Applied Mathematics, SIAM. Chapter 4 covers the conditioning–accuracy trade-off in detail.</p></li><li><p><strong>Fornberg, B. and Wright, G.</strong> (2004). <em>Stable computation of multiquadric interpolants for all values of the shape parameter.</em> Computers &amp; Mathematics with Applications, <strong>48</strong>(5–6), 853–867. DOI: <a href="https://doi.org/10.1016/j.camwa.2003.08.010" target="_blank" rel="noreferrer">10.1016/j.camwa.2003.08.010</a>.</p></li><li><p><strong>Fornberg, B., Larsson, E., and Wright, G.</strong> (2004). <em>A new class of oscillatory radial basis functions.</em> Computers &amp; Mathematics with Applications, <strong>51</strong>(8), 1209–1222. DOI: <a href="https://doi.org/10.1016/j.camwa.2006.04.004" target="_blank" rel="noreferrer">10.1016/j.camwa.2006.04.004</a>.</p></li></ol>',28)])])}const b=a(c,[["render",d]]);export{f as __pageData,b as default};
