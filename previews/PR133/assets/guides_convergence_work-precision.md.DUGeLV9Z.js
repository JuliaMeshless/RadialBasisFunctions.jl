import{_ as a,c as o,o as t,aA as i}from"./chunks/framework.Bx7xRqet.js";const r="/RadialBasisFunctions.jl/previews/PR133/assets/work_precision_laplacian_build.y7tc9R-s.png",s="/RadialBasisFunctions.jl/previews/PR133/assets/work_precision_laplacian_apply.B6f1_EPC.png",n="/RadialBasisFunctions.jl/previews/PR133/assets/work_precision_memory.Bc9FyLEj.png",c="/RadialBasisFunctions.jl/previews/PR133/assets/work_precision_interpolation_build.B1v_s4bX.png",l="/RadialBasisFunctions.jl/previews/PR133/assets/work_precision_interpolation_apply.BKzdbrhb.png",b=JSON.parse('{"title":"Work-Precision","description":"","frontmatter":{},"headers":[],"relativePath":"guides/convergence/work-precision.md","filePath":"guides/convergence/work-precision.md","lastUpdated":null}'),p={name:"guides/convergence/work-precision.md"};function d(h,e,u,m,g,y){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Work-Precision" tabindex="-1">Work-Precision <a class="header-anchor" href="#Work-Precision" aria-label="Permalink to &quot;Work-Precision {#Work-Precision}&quot;">​</a></h1><p>Convergence plots tell you <em>which</em> basis and polynomial degree are most accurate per point. Work-precision plots tell you <em>what they cost</em>. This page uses <code>laplacian</code> as a surrogate operator — the weight-assembly cost is nearly identical across local-stencil differential operators since they share the same local-system structure — and treats <code>Interpolator</code> separately because it uses a global stencil.</p><p>Each marker represents one <code>(basis, poly_deg)</code> configuration; connected markers are the same configuration at different <code>N ∈ {225, 900, 2025, 4900}</code>. The lower-left corner is &quot;best&quot;: low error per unit time.</p><p>Plots below are <strong>PHS-only</strong>. Shape-parameter bases (<code>IMQ</code>, <code>Gaussian</code>) need ε scaled with stencil spacing to produce a fair cost comparison (see <a href="./shape-parameter-bases">Shape-Parameter Bases</a> for why); a cost study of the scaled-ε configs is not covered here.</p><div class="tip custom-block"><p class="custom-block-title">Benchmark environment</p><p>Timings below were measured on: AMD Ryzen 9 9900X, 20 threads, Julia 1.12.6, RadialBasisFunctions 0.5.0. Absolute numbers will differ on other machines; the <em>relative</em> ordering of basis configurations is the informative part.</p></div><h2 id="Weight-build-time-—-Laplacian" tabindex="-1">Weight build time — Laplacian <a class="header-anchor" href="#Weight-build-time-—-Laplacian" aria-label="Permalink to &quot;Weight build time — Laplacian {#Weight-build-time-—-Laplacian}&quot;">​</a></h2><p><img src="'+r+'" alt=""></p><p>Each family appears twice: a solid curve at the matched polynomial degree (<code>PHS3/p=2</code>, <code>PHS5/p=3</code>, <code>PHS7/p=4</code>) and a dashed curve at a higher-poly_deg overshoot (<code>p=4</code>, <code>p=5</code>, <code>p=6</code> respectively). The overshoot curves sit below their matched siblings — extra polynomial degree buys lower error at extra cost. Higher-order PHS dominates the Pareto frontier: <code>PHS7/p=6</code> reaches the lowest error, <code>PHS5/p=3</code> hits the sweet spot for most applications. <code>PHS1/p=1</code> is omitted because its error is not useful for second-derivative operators (see the <a href="/RadialBasisFunctions.jl/previews/PR133/guides/convergence/scalar-operators#laplacian">Laplacian section</a>).</p><h2 id="Apply-time-—-Laplacian" tabindex="-1">Apply time — Laplacian <a class="header-anchor" href="#Apply-time-—-Laplacian" aria-label="Permalink to &quot;Apply time — Laplacian {#Apply-time-—-Laplacian}&quot;">​</a></h2><p><img src="'+s+'" alt=""></p><p>Apply time is 10–100× shorter than build time; once weights are built, applying the operator is a sparse matrix-vector product. This makes the &quot;amortize over many applies&quot; decision easy: if you apply the same operator many times (e.g., time-stepping a PDE), pick the highest-accuracy configuration you can afford during the one-time build.</p><h2 id="Memory-footprint-—-Laplacian" tabindex="-1">Memory footprint — Laplacian <a class="header-anchor" href="#Memory-footprint-—-Laplacian" aria-label="Permalink to &quot;Memory footprint — Laplacian {#Memory-footprint-—-Laplacian}&quot;">​</a></h2><p><img src="'+n+'" alt=""></p><p>Memory scales with <code>N × k × (k + npoly)</code> — the local-system factorization cost. Higher <code>poly_deg</code> inflates both the stencil requirement and the factorization size.</p><h2 id="Interpolation" tabindex="-1">Interpolation <a class="header-anchor" href="#Interpolation" aria-label="Permalink to &quot;Interpolation {#Interpolation}&quot;">​</a></h2><p>The <code>Interpolator</code> uses a global stencil: every evaluation touches every data point. Cost scales very differently from local-stencil operators — the build cost is dominated by a single <code>N×N</code> dense factorization, and the per-evaluation cost is <code>O(N)</code>.</p><h3 id="Build-time" tabindex="-1">Build time <a class="header-anchor" href="#Build-time" aria-label="Permalink to &quot;Build time {#Build-time}&quot;">​</a></h3><p><img src="'+c+'" alt=""></p><h3 id="Apply-time" tabindex="-1">Apply time <a class="header-anchor" href="#Apply-time" aria-label="Permalink to &quot;Apply time {#Apply-time}&quot;">​</a></h3><p><img src="'+l+'" alt=""></p><p>For scattered-data interpolation over more than a few thousand points, this global approach becomes costly. Use <code>regrid</code> (local-stencil interpolation between point sets) when you need to transfer data at scale.</p><h2 id="Practical-guidance" tabindex="-1">Practical guidance <a class="header-anchor" href="#Practical-guidance" aria-label="Permalink to &quot;Practical guidance {#Practical-guidance}&quot;">​</a></h2><ul><li><p><strong>Default</strong>: <code>PHS(3; poly_deg=2)</code> — 2nd-order accurate, no tuning, well within 1 decade of the Pareto frontier on every plot above.</p></li><li><p><strong>When you need more accuracy</strong>: <code>PHS(5; poly_deg=3)</code> typically halves the error at ~3× the build cost. Beyond that, PHS7/p=4 offers diminishing returns unless you&#39;re targeting <code>&lt; 10⁻⁶</code> error.</p></li><li><p><strong>When build cost dominates</strong>: compute once, apply many times. The apply-time plot is roughly 10–100× faster than build.</p></li><li><p><strong>When you need mixed partials or the full Hessian</strong>: start at PHS5/p=3 regardless of cost — the matched PHS3/p=2 does not converge (see the <a href="/RadialBasisFunctions.jl/previews/PR133/guides/convergence/scalar-operators#mixed-partial-xxj">scalar operators</a> section).</p></li></ul>',23)])])}const _=a(p,[["render",d]]);export{b as __pageData,_ as default};
